doctype html
html(lang="en")
    head
        title #{pageTitle}
    body 
        a(href="/") home
        h1=notice.title
        p=notice.content
        br
        a(href=`${notice.id}/edit`) Edit
        br
        a(href=`${notice.id}/delete`) Delte