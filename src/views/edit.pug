doctype html
html
    head
        title #{pageTitle}
    body 
        h1 edit
        form(method="post")
            input(type="text", name="title", placeholder="제목", required, value=`${notice.title}`)
            input(type="text", name="content", placeholder="내용", required, value=`${notice.content}`)
            input(type="submit",value="submit")